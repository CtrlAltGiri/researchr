- Add DateTime selectors as needed.
- Allow URL inputs for companies to get logo.
- Tag Input has to be modified as its own component to allow tag-inputs
- API for tag input should also be created appropriately.